---
verdict: ПОД ВОПРОСОМ
checked: 2026-09-25T18:05:00+03:00
approved: 2026-09-25T18:41:16+03:00
---

## Что обещаем покупателю (одной фразой, с ценой)
Голосовое или текст в Telegram-бот @cyberdub_second_brain_bot превращается в связанные заметки
Obsidian-хранилища, круглосуточно на .44, по подписке Claude Max без доплаты за токены.
Покупатель — один: владелец (личный инструмент, не продаётся).

## Путь покупателя
| Шаг | Что должно быть | Что есть | Работает/нет | Чем проверено: URL/скрин/вывод |
| --- | --- | --- | --- | --- |
| 1. Бот принимает сообщение | `dbrain-bot.service` active, polling | лежал с 2026-09-22 04:06 (исчерпан StartLimitBurst=5/300s); 25.09 17:56 поднят вручную | нет → да после ручного старта | `systemctl --user status dbrain-bot` → `failed … 3 days ago`; после `start`: `Run polling for bot @cyberdub_second_brain_bot` |
| 2. Бот передаёт текст мозгу | промпт вставлен и отправлен Enter | промпт вставляется, Enter не срабатывает: 6 канареек склеены в одну неотправленную строку | **нет** | `tmux capture-pane -t dbrain_d82760f4` |
| 3. Мозг отвечает | ответ между маркерами `<<<R:…>>>`/`<<<E:…>>>` | ответа нет | **нет** | doctor локально: `❌ canary: no reply in 120.0s`; `dbrain-doctor.service` failed ежедневно |
| 4. Заметка ложится в хранилище | новые файлы в vault | последняя запись 2026-07-12 | нет свежих | `find vault -type f -printf '%TY-%Tm-%Td'` |
| 5. Обработка 21:00 | разбор дня, граф | вхолостую: `empty daily, graph-only` | формально да | `journalctl --user -u dbrain-process` 24.09 |
| 6. Doctor 08:00 шлёт тревогу | 🔴 в Telegram | шлёт, но не чинит | сигнал да, восстановление нет | `dbrain-doctor.service` failed 25.09 08:02 |

## Результат против требований
- Годится: транспорт Telegram через прокси, ночная обработка, doctor-тревога, изоляция `CLAUDE_CONFIG_DIR`.
- Не годится: главный контур «сообщение → ответ мозга» мёртв; бот не переживает серию падений; watchdog не видит зависший ввод.

## Вердикт
ПОД ВОПРОСОМ

Причины:
1. Ядро не отвечает: `tmux paste-buffer` без `-p` + Enter через 0.3 с — Claude Code 2.1.278+ проглатывает Enter как часть вставки. [UNVERIFIED] гипотеза, фикс `paste-buffer -p`.
2. Бот 3 дня лежал незамеченным: `Restart=always` упирается в `StartLimitBurst`. Причина падения 22.09 04:06 не установлена (журнал ротирован; `uv cache prune` опровергнут — он вс 05:15).
3. Владелец не пользовался с 2026-07-12.

## До продажи
- [ ] Отправка промпта: bracketed paste + Enter, тест + живая канарейка ✅ (2 ч)
- [ ] Бот не сдаётся после серии падений (`StartLimitIntervalSec=0`/нарастающий RestartSec) (1 ч)
- [ ] Watchdog распознаёт «промпт висит в поле ввода» (1,5 ч)
- [ ] Свести `main` с `origin/main` (ahead 12, behind 1) (0,5 ч)

## Что изменилось для покупателя за прошлый прогон
- бот снова принимает сообщения (поднят 25.09 17:56), ответов мозга пока нет
