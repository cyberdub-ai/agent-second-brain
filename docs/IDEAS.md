# IDEAS — agent-second-brain

## 2026-09-25

Расстояние до прода: открыт только A-04 (push `main` в `fork` — за владельцем, сторож отбил). Раздела
«Гейт готовности» и `docs/PROD-READINESS.md` нет.

Что вскрылось за ночь: `origin` — чужой upstream smixs, критерий A-04 про `origin/main..HEAD` недостижим;
с `StartLimitIntervalSec=0` `OnFailure=` на серию падений бота больше не срабатывает; в cgroup watchdog
остался `curl` прошлого запуска; SCOPE-FENCE считает панель бота `dbrain_d82760f4` сессией проекта.
Macroscope: открытых PR нет.

- Doctor тревожит при серии рестартов бота (`NRestarts` вырос за сутки) — замена потерянного `OnFailure=` · надёжность · S · **в черновик** · одна проверка в doctor + тест · к проду: путь покупателя шаг 1 (бот 3 дня лежал незамеченным)
- Doctor проверяет local Whisper (`check_whisper`: HTTP-пинг `WHISPER_URL`) — голос обещан покупателю и вживую не проверен · надёжность · S · **в черновик** · одна проверка + тест · к проду: путь покупателя шаг 1 (голосовое)
- Критерий A-04 перевести на `fork/main..HEAD` = 0 · поддержка · S · в бэклог · решение владельца, не работа агента · к проду: закрывает A-04 (формулировка)
- Лог бота в файл с ротацией дольше journald — причина падения 22.09 потеряна вместе с журналом · поддержка · S · в бэклог · к проду: нет
- `ExecStopPost`/`KillMode` watchdog: не оставлять `curl` в cgroup · надёжность · S · в бэклог · к проду: нет
- SCOPE-FENCE: исключить сессии `dbrain_*` (хостовый гард, не проект) · поддержка · S · в бэклог · к проду: нет

## 2026-09-25, цикл 2

Расстояние до прода: из ACCEPTANCE открыт только A-04 (push в `fork`, за владельцем). Гейт готовности
(`docs/PROD-READINESS.md`) 2/10 ✓: не закрыты функционал, SEO, дизайн, скорость, безопасность, бэкапы,
юр. страницы, деплой и откат.

Что вскрылось за ночь: Macroscope — открытых PR нет. `scripts/process.sh` (таймер `dbrain-process`,
21:00) делает `git add -A` по всему репо продукта и `git push || true` в `origin` = чужой smixs:
три коммита «chore: process daily» уже унесли недоделанный код разработчика (`c26651b`, `baa106e`,
`6052942`), push молча падает. Личных заметок в коммитах нет — их держит `.gitignore`, но любой новый
каталог vault вне списка уйдёт в публичный `fork` при следующем push. Заметки пользователя (`vault/daily`
и др.) при этом не бэкапятся никуда. Whisper на холодной модели ответил за 112 с при таймауте
транскрибера 120 с. README.ru.md 5 раз обещает Deepgram, а голос в форке идёт в локальный Whisper.

- `process.sh` коммитит только vault (`git add -A -- vault`) и не пушит в `origin` · безопасность · S · **в черновик** · две строки + тест скрипта · к проду: гейт «безопасность»
- Ежедневный снимок vault (tar в `/srv/data/dbrain/backup`, хранить 14) + `check_backup` в doctor на свежесть ≤26 ч · надёжность · M · **в черновик** · таймер + проверка + тест · к проду: гейт «бэкапы»
- README.ru.md: приватность и голос — локальный Whisper вместо Deepgram · юр./доверие · S · **в черновик** · правка текста · к проду: гейт «юр. страницы»
- Таймаут транскрибера `WHISPER_TIMEOUT` = 300 с: холодный large-v3 отвечает 112 с при потолке 120 · скорость · S · **в черновик** · одна константа + тест · к проду: гейт «скорость»
- SEO и дизайн для Telegram-бота — отметить «н/п» в гейте готовности · поддержка · S · в бэклог · решение владельца · к проду: гейт «SEO», «дизайн» (формулировка)
- Прогрев Whisper после старта контейнера (тишина-WAV в healthcheck) · скорость · S · в бэклог · перекрыт таймаутом · к проду: нет
